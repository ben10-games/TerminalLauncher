<?xml version="1.0" encoding="utf-8"?>
<!--
    Adaptive icon background layer: a solid, near-black fill matching
    the terminal's own background. Deliberately plain (no pattern) so
    the ">_" foreground glyph reads cleanly at every launcher icon
    shape/mask.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#0D0D0D"
        android:pathData="M0,0h108v108h-108z" />
</vector>
